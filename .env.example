# Employee login (Clerk) — copy this file to `.env.local` and fill it in.
#
# `.env.local` is gitignored. This template is the exception (see .gitignore),
# so it must never contain a real value.
#
# Without these, the site runs exactly as it does today: no login link, and
# /portal, /sign-in and /sign-up return 404. Nothing else changes.

# From Clerk dashboard → API keys. Safe to expose: it ships in the browser
# bundle by design.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_replace-me

# From the same page. SECRET — server-side only.
#
#   * Never put this in a NEXT_PUBLIC_* variable. Anything prefixed
#     NEXT_PUBLIC_ is compiled into the JavaScript every visitor downloads,
#     and a leaked Clerk secret key is full account compromise.
#   * Never paste it into a chat, an issue, or a commit.
CLERK_SECRET_KEY=sk_test_replace-me

# Where Clerk's own flows live in this app. `auth.protect()` reads
# CLERK_SIGN_IN_URL to know where to send an unauthenticated visitor.
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up

# Where to land after signing in or accepting an invitation. These are the
# current names — the older AFTER_SIGN_IN_URL / AFTER_SIGN_UP_URL were removed.
NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL=/portal
NEXT_PUBLIC_CLERK_SIGN_UP_FALLBACK_REDIRECT_URL=/portal

# Note on environments: pk_test_/sk_test_ keys work on any domain and must
# never reach production. Production needs a separate Clerk production
# instance with its own pk_live_/sk_live_ pair and a configured domain.

# --- Contact form (Resend) ----------------------------------------------------
#
# Without these the contact page still renders, but submitting returns a clear
# error telling the visitor to email you instead — never a silent failure that
# makes them think they got through.
#
# 1. Sign up at resend.com (free tier is plenty)
# 2. API Keys -> Create -> copy it below
# 3. Restart the dev server

# SECRET. Server-side only, never NEXT_PUBLIC_, never pasted into a chat.
RESEND_API_KEY=

# Where enquiries land.
CONTACT_TO_EMAIL=

# Who they appear to come from. Until you verify a domain in Resend, leave this
# as the shared sandbox address — with it, Resend will only deliver to the
# address that owns the Resend account.
CONTACT_FROM_EMAIL=onboarding@resend.dev

# --- WhatsApp assistant (Cloud API) — NOT IN USE ------------------------------
#
# Vamscore handles WhatsApp in the WhatsApp Business app: the website's
# WhatsApp button is a plain wa.me link, its number lives in
# content/contact.ts, and the auto-reply is the app's Greeting message. None of
# that needs anything below.
#
# What follows powers the optional AI assistant and staff inbox built on the
# WhatsApp Cloud API. It stays off until filled in: the webhook returns 503 and
# the inbox shows "not connected". A half-filled set counts as off. Note that
# connecting a number directly to the Cloud API ("meta") removes it from the
# WhatsApp Business phone app.

# Database (Neon Postgres — add it from the Vercel Marketplace, which sets this
# for you). Then run `npm run db:migrate` once to create the tables.
# SECRET. Server-side only.
DATABASE_URL=

# Which route to WhatsApp: "meta" (direct to Meta — use for a new number, or to
# move 9490729484 fully onto the dashboard) or "360dialog" (only to keep the
# number on the WhatsApp Business phone app as well).
WHATSAPP_PROVIDER=

# --- WHATSAPP_PROVIDER=meta
# From the Meta developer app → WhatsApp → API Setup.
WHATSAPP_PHONE_NUMBER_ID=
# SECRET. A permanent System User token (Business Settings → System users), not
# the 24-hour temporary token from the API Setup page.
WHATSAPP_ACCESS_TOKEN=
# SECRET. App settings → Basic → App secret. Used to verify every webhook.
WHATSAPP_APP_SECRET=
# Any long random string you choose; paste the same value into Meta's webhook
# settings as the "Verify token".
WHATSAPP_VERIFY_TOKEN=
# Optional. Graph API version; defaults to v26.0.
WHATSAPP_GRAPH_VERSION=

# --- WHATSAPP_PROVIDER=360dialog
# SECRET. From the 360dialog hub, per phone number.
D360_API_KEY=
# SECRET. Long random string; register the webhook as
#   https://vamscore.com/api/whatsapp/webhook?token=<this value>
WHATSAPP_WEBHOOK_SECRET=

# --- The assistant (Claude)
# SECRET. From console.anthropic.com. Without it, WhatsApp still works but every
# chat goes straight to staff — the assistant simply stays off.
ANTHROPIC_API_KEY=

# Where WhatsApp alerts go (handoffs, leads, call-backs). Defaults to
# CONTACT_TO_EMAIL. Uses the Resend settings above.
WHATSAPP_ALERT_TO=
