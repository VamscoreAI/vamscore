/**
 * The site's one public origin. Canonical URLs, the sitemap, robots.txt and the
 * structured data all build absolute URLs from this, so search engines see a
 * single address for every page — never `www.`, never a `*.vercel.app` host.
 */
export const SITE_URL = "https://vamscore.com";
